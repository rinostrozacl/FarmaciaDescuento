import { defineNuxtPlugin } from "#app";

// Importamos los estilos de Font Awesome
import "@fortawesome/fontawesome-free/css/all.css";

export default defineNuxtPlugin((nuxtApp) => {
  // No necesitamos hacer nada más en el plugin,
  // solo cargar los estilos CSS de Font Awesome
});
